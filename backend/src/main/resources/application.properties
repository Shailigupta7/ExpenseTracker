spring.main.allow-circular-references=true

# ================= DATABASE =================
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3308/expense_tracker?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ================= JWT =================
app.jwtSecret=${JWT_SECRET:======================expense=tracker=mywallet===========================}
app.jwtExpirationMs=86400000

# ================= EMAIL =================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:shaili07gupta@gmail.com}
spring.mail.password=${MAIL_PASSWORD:xdfi fmxo nuse jpht}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ================= OTHER =================
app.verificationCodeExpirationMs=60000
app.user.profile.upload.dir=./uploads/user/profile
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
